# Source Files

Store optional local copies of source PDFs or supplementary files here when continued review needs them.

Use the same slug as the corresponding paper note:

- `YYYY_first-author_short-slug.pdf`

Prefer keeping the original source link in the paper note even when a local copy exists.
